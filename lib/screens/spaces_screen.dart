import 'package:flutter/material.dart';

import '../widgets/spaces_screen/spaces.dart';

class SpacesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Spaces(),
    );
  }
}
