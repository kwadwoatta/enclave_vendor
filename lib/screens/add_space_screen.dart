import 'package:flutter/material.dart';

import '../widgets/add_space_screen/add_space.dart';

class AddSpaceScreen extends StatelessWidget {
  static const routeName = '/add-space-screen';

  @override
  Widget build(BuildContext context) {
    return AddSpace();
  }
}
